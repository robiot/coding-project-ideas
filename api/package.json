{
    "name": "api",
    "private": true,
    "version": "1.0.0",
    "description": "package for creating workers templates",
    "main": "./src/index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'",
        "publish": "wrangler publish",
        "dev": "wrangler preview --watch"
    },
    "author": "robiot <robiot@users.noreply.github.com>",
    "license": "ISC",
    "devDependencies": {
        "prettier": "^1.17.0"
    },
    "dependencies": {
        "itty-router": "^2.1.9",
        "serverless-cloudflare-workers": "^1.2.0",
        "uuid": "^8.3.2"
    }
}
